// グローバル変数を定義するときは、変数名の頭に"作者名_"を追加
var yourname_var = 0;

function yourname_setup() {
  cnv = createCanvas(1280, 720); // 描画領域の大きさは、必要に応じて変更
}

function yourname_draw() {
  push();
  
  // 以下、作品のコード
  
  
  pop();
}
